# ── Core ───────────────────────────────────────────────────
flask==3.1.3
python-dotenv==1.2.2

# ── NLP / ML ────────────────────────────────────────────────
spacy==3.8.14
nltk==3.9.4
pandas==3.0.2
scikit-learn==1.8.0
numpy==2.4.4

# ── Utilities ───────────────────────────────────────────────
requests==2.33.1
sqlalchemy==2.0.49

# ── AI Analysis providers (install at least one) ────────────
anthropic>=0.40.0       # Claude API              pip install anthropic
openai>=1.50.0          # OpenAI + Whisper cloud   pip install openai

# ── Local transcription (Whisper) ───────────────────────────
faster-whisper>=1.0.0   # Whisper inference        pip install faster-whisper
nvidia-cublas-cu12      # CUDA support on Windows  pip install nvidia-cublas-cu12
                        # (omit if CPU-only or non-Windows)

# ── Dev / testing ───────────────────────────────────────────
pytest==9.0.3
pytest-cov==7.1.0
black==26.3.1
flake8==7.3.0
